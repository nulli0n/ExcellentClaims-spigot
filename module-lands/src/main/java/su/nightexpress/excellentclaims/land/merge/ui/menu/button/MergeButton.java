package su.nightexpress.excellentclaims.land.merge.ui.menu.button;

import org.jspecify.annotations.NullMarked;

@NullMarked
public class MergeButton {

    /*
                                           this.addDefaultButton("merge", MenuItem.builder()
                               .defaultState(ItemState.defaultBuilder()
                               .icon(NightItem.fromType(Material.WATER_BUCKET)
                               .setDisplayName(BLUE.wrap(BOLD.wrap("Merge Claim")))
                               .setLore(Lists.newList(
                               GRAY.wrap("Merge chunks(s) of this claim"),
                               GRAY.wrap("into another claim."),
                               "",
                               BLUE.wrap("→ " + UNDERLINED.wrap("Click to merge"))
                               ))
                               .hideAllComponents()
                               )
                               .condition(context -> this.createPermissionCondition(context, ClaimPermission.MERGE_CLAIM))
                               .action(this.createObjectAction(context -> this.handleMerge(context, MergeType.MERGE)))
                               .build()
                               )
                               .slots(51)
                               .build()
                               );
    */
}
