package su.nightexpress.excellentclaims.land.merge.ui.menu.button;

import org.jspecify.annotations.NullMarked;

@NullMarked
public class SplitButton {

    /*
                                   this.addDefaultButton("separate", MenuItem.builder()
                               .defaultState(ItemState.defaultBuilder()
                               .icon(NightItem.fromType(Material.LAVA_BUCKET)
                               .setDisplayName(ORANGE.wrap(BOLD.wrap("Split Claim")))
                               .setLore(Lists.newList(
                               GRAY.wrap("Separate a chunk of this"),
                               GRAY.wrap("claim as a dedicated claim."),
                               "",
                               ORANGE.wrap("→ " + UNDERLINED.wrap("Click to separate"))
                               ))
                               .hideAllComponents()
                               )
                               .condition(context -> this.createPermissionCondition(context, ClaimPermission.SPLIT_CLAIM))
                               .action(this.createObjectAction(context -> this.handleMerge(context, MergeType.SPLIT)))
                               .build()
                               )
                               .slots(47)
                               .build()
                               );
    */
}
